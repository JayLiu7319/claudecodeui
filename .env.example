# Claude Code UI 环境变量配置
# 仅包含代码中实际使用的变量
#
# 提示：运行 'cloudcli status' 查看此文件应放置的位置
#      并查看当前的配置信息。
#
# 可用的 CLI 命令：
#   claude-code-ui    - 启动服务器（默认）
#   cloudcli start    - 启动服务器
#   cloudcli status   - 显示配置和数据存储位置
#   cloudcli help     - 显示帮助信息
#   cloudcli version  - 显示版本信息

# =============================================================================
# 服务器配置 (SERVER CONFIGURATION)
# =============================================================================

# 后端服务器端口（包含 Express API 和 WebSocket 服务）
# API 服务器端口
PORT=3001
# 前端开发服务器端口
VITE_PORT=5173

# 如果你的 claude cli 路径不是默认的 "claude"，请取消下方行的注释并修改
# CLAUDE_CLI_PATH=claude

# =============================================================================
# 数据库配置 (DATABASE CONFIGURATION)
# =============================================================================

# 身份验证数据库文件路径
# 用于存储用户凭据、API 密钥和 Token 等信息。
#
# 如需使用自定义路径，请取消注释：
# DATABASE_PATH=/path/to/your/custom/auth.db
#
# Claude Code 上下文窗口大小（每个会话的最大 Token 数）
VITE_CONTEXT_WINDOW=160000
CONTEXT_WINDOW=160000

# VITE_IS_PLATFORM=false
